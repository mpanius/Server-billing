services:
  server-billing:
    build: .
    ports:
      - "8000:8000"
    environment:
      APP_NAME: Server Billing Manager
      DATABASE_PATH: /app/data/server_billing.db
      BASE_URL: http://127.0.0.1:8000
    volumes:
      - ./data:/app/data
